function fn() {
    var config = {
        // Directly set the baseUrl for your API
        baseUrl: 'http://localhost:3100/api',
         timeout: 10000 // Set the timeout to 10 seconds (10000 milliseconds)
        
    };
    return config;
}
